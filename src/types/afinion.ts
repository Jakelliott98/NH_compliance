

export interface AfinionDatabaseType {
    afinion_id: number, 
    created_at: Date,
    last_calibrated: string,
    afinion_number: number,
    nh_number: string,
    site_id: number,
    last_clean: Date,
}